Importants things noticed while solving the questions:

BINARY SEARCH
  in rotated sorted array -> you can only search the sorted part 
    figure out how to find the sorted array and search the element

HASH MAPS
  you can directly compare hashmaps by using the == op
  also using array to count freq is better

